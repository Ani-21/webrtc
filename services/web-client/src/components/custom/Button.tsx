import { Button, styled } from '@mui/material';

export const CustomButton = styled(Button)(() => ({
  backgroundColor: '#2C68FA',
  borderRadius: '11px',
  padding: '12px 28px',
  fontSize: '16px',
  lineHeight: '24px',
  color: '#ffffff',
}));
